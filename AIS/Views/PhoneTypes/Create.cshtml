@model AISModels.PhoneTypes
@using (Ajax.BeginForm("Create", "PhoneTypes", null, new AjaxOptions() { HttpMethod = "POST", OnSuccess = "refreshContent" }, new { @id = "ModalForm" }))
{ 
    @Html.ValidationSummary(true)
    <div class="for-edit">
        <h1 id="title">
            Add PhoneType</h1>
        <input type="button" value="Cancel" onclick="cancelClick()" id="btnCancel" />
        <input type="submit" value="Save" class="saveBtn svbtn" />
    </div>
    <div class="add-new-main content_1">
        <div class="add-new-inner">
            <div class="add-new-sec-1">
                <label>
                    Phone Type</label>
                @Html.TextBoxFor(model => model.PhoneType)
                <br />
                @Html.ValidationMessageFor(model => model.PhoneType)
                <span id="aerror" style="color: Red"></span>
            </div>
        </div>
    </div>
}
