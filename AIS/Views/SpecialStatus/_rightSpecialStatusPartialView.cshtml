@model IEnumerable<AIS.Models.AlphabeticalMapping<AISModels.SpecialStatus>>
<div class="main-right-sec-in">
    @foreach (var oitem in Model)
    {
        <div class="section-head">
            <p>
                @oitem.FirstLetter</p>
        </div>
        foreach (var item in oitem.Items)
        {
        <ul class="people-status">
            <li id="@item.SpecialStatusId">
                <img alt="" src="../../Images/available-icon.png" />
                <h1 class="allergyshow">@item.Status</h1>
            </li>
        </ul>
        }
    }
</div>
