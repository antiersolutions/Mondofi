@model IEnumerable<AIS.Models.AlphabeticalMapping<AISModels.Cities>>
<div class="main-right-sec-in">
    @foreach (var oitem in Model)
    {
        <div class="section-head">
            <p>
                @oitem.FirstLetter</p>
        </div>
        foreach (var item in oitem.Items)
        {
        <ul class="people-status">
            <li id="@item.CityId">
                <img alt="" src="../../Images/available-icon.png" />
                <h1 class="allergyshow">@item.City</h1>
            </li>
        </ul>
        }
    }
</div>
